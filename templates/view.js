import { **Template**Templates } from './template.js';

export function **Template**View() {
  this.template = new **Template**Templates();
  
  // This is where you define all the html nodes you will be working with
  // this.body = document.getElementById('**insert id here**');
}

**Template**View.prototype.bind = function (event, handler) {

}

**Template**View.prototype.render = function () {

}
